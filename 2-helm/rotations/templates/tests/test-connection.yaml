apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rotations.fullname" . }}-test-connection"
  labels:
    {{- include "rotations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rotations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
