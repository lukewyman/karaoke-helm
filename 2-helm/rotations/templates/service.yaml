apiVersion: v1
kind: Service
metadata:
  name: {{ include "rotations.fullname" . }}
  labels:
    {{- include "rotations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rotations.selectorLabels" . | nindent 4 }}
