apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "singers.fullname" . }}-test-connection"
  labels:
    {{- include "singers.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "singers.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
