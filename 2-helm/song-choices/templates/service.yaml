apiVersion: v1
kind: Service
metadata:
  name: {{ include "song-choices.fullname" . }}
  labels:
    {{- include "song-choices.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "song-choices.selectorLabels" . | nindent 4 }}
